name: Build ARM64 DLL

on:
  push:
    branches: [ "rust" ]
  workflow_dispatch: # Allows you to trigger the build manually

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      # The --workspace flag ensures the 'dll' sub-project is compiled
      - name: Build x64 target
        run: cargo build --release --workspace --target x86_64-pc-windows-msv

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: VirtualDesktopAccessor-x64
          # Grab any DLL outputted into the release folder
          path: target/x86_64-pc-windows-msvc/release/*.dll
